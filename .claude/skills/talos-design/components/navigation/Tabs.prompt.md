Underline tab strip for detail-page navigation (Project Detail, Run Detail).

```jsx
<Tabs
  items={[{ key: "overview", label: "Overview" }, { key: "runs", label: "Runs" }]}
  activeKey={tab}
  onChange={setTab}
/>
```

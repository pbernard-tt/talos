Identifies the agent/provider behind a run, task, or integration row.

```jsx
<AgentBadge agentKey="claude-code" />
<AgentBadge agentKey="codex-cli" />
```

Add new adapters here (and nowhere else) as the roster grows — the plan's adapter list is fixed order: Custom Shell → Claude Code → OpenCode → Codex CLI → OpenHands → (backlog) Gemini CLI.

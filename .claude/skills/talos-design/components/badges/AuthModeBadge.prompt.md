Discloses which credential mode a run or integration used.

```jsx
<AuthModeBadge mode="subscription_local" />
<AuthModeBadge mode="api_key" />
```

Always show this next to cost figures — a `subscription_local` run's dollar cost is genuinely unreported, never estimated.

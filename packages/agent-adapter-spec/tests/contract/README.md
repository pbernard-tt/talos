Contract test suite for `AgentAdapter` implementations (Section 7.2 of the implementation plan).

Populated in Phase 6 alongside the `AgentAdapter` ABC and `CustomShellAdapter`. Every adapter
(including `ClaudeCodeAdapter` in Phase 7) must pass this suite before registration.

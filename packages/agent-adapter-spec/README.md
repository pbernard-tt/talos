# Talos Agent Adapter Specification

The public Python interface, shared data types, and contract tests used to integrate supported
coding-agent providers with Talos. Provider-specific coupling is contained behind this package's
`AgentAdapter` interface.

Copyright 2026 Vulkan Technologies.

## Licensing

This standalone reusable integration library is licensed under the
[Apache License 2.0](LICENSE) (`Apache-2.0`). It is an explicit exception to the repository root's
AGPL licence and does not inherit `AGPL-3.0-or-later` merely because it is maintained in this
monorepo.

# packages/project-config-schema

Will hold `talos.schema.json`, the JSON Schema for `talos.yaml` (Section 14 of the implementation plan). Built in Phase 3 alongside project registry config parsing and `sync-config` validation.

Copyright 2026 Vulkan Technologies.

## Licensing

The reusable Talos project-configuration schema is licensed under the
[Apache License 2.0](LICENSE) (`Apache-2.0`). This directory is an explicit exception to the
repository root's AGPL licence.

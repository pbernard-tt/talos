# packages/project-config-schema

Will hold `talos.schema.json`, the JSON Schema for `talos.yaml` (Section 14 of the implementation plan). Built in Phase 3 alongside project registry config parsing and `sync-config` validation.
